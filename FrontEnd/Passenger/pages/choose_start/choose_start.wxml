<!--pages/choose_start/choose_start.wxml-->
<view class="section">
  <input bindinput="bindInput" placeholder="搜索" focus="true" />
</view>
<view bindtouchstart="bindSearch" data-keywords_s="{{item.name}}" class="text_box" wx:for="{{tips}}" wx:key="key">
  {{item.name}}
</view>
